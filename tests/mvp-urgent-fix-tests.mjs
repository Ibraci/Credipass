import assert from 'node:assert/strict';import fs from 'node:fs';
const html=fs.readFileSync(new URL('../index.html',import.meta.url),'utf8');
const app=fs.readFileSync(new URL('../src/az-app.js',import.meta.url),'utf8');
const sw=fs.readFileSync(new URL('../sw.js',import.meta.url),'utf8');
const launch=fs.readFileSync(new URL('../LANCER_MVP_DEMO_CORRIGE.bat',import.meta.url),'utf8');
assert.ok(html.includes('id="copilotFab" class="copilot-fab" type="button"'),'FAB direct');
assert.ok(!/id="copilotFab"[^>]*data-act=/.test(html),'FAB ne doit pas cumuler data-act + listener direct');
assert.match(app,/\$\('#copilotFab'\)\?\.addEventListener\('click'/,'listener direct Copilot');
assert.match(sw,/credipass-r19-3-2-postgresql-central/,'cache PWA coherent');
assert.doesNotMatch(launch,/reset-demo-accounts\.mjs/,'launcher ne doit pas écraser un mot de passe utilisateur modifié');
assert.ok(fs.existsSync(new URL('../PREPARER_COMPTES_DEMO.bat',import.meta.url)),'réinitialisation manuelle des comptes disponible');
console.log('MVP URGENT FIX: PASS — Copilot one-click + comptes persistants + reset manuel + cache bust');
