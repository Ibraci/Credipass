const ENGINE_VERSION='4.0.0';
export function buildRegionalOperations({countries,tenants,environments,security,modelValidation}={}){
  const countryRows=countries.map(c=>{const ts=tenants.tenants.filter(t=>t.countryCode===c.countryCode);return{...c,tenantCount:ts.length,policyIds:[...new Set(ts.map(t=>t.policyId))],productionEnabled:false};});
  const envs=environments.map(e=>{if(e.id==='demo')return{...e,allowed:true};if(e.id==='pilot')return{...e,allowed:security.pilotAllowed&&modelValidation.registry.productionValidated===false?false:security.pilotAllowed};return{...e,allowed:false};});
  return {engineVersion:ENGINE_VERSION,countries:countryRows,environments:envs,release:{channel:'demo',version:'4.0.0',configurationVersion:'REGIONAL-CONFIG-1',rollbackPrepared:true,automaticProductionPromotionAllowed:false},continuity:{rtoTargetHours:8,rpoTargetHours:24,backupRestoreStatus:'not-executed-on-real-server',tabletopExerciseOnly:true},legal:{countryRulesStatus:'templates-only-to-validate',dataResidencyStatus:'to-be-agreed-per-institution',regulatoryCertificationClaimed:false},summary:{configuredCountries:countryRows.length,configuredTenants:tenants.summary.tenantCount,pilotAllowed:security.pilotAllowed,productionAllowed:false},safeguards:{productionDeploymentAllowed:false,realCustomerDataInDemoAllowed:false,automaticCrossCountryDataTransferAllowed:false,localLegalValidationRequired:true}};
}
export function validateRegionalOperations(r){return Boolean(r?.countries?.length===5&&r?.summary?.productionAllowed===false&&r?.safeguards?.automaticCrossCountryDataTransferAllowed===false&&r?.legal?.regulatoryCertificationClaimed===false);}
