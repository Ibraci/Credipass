export async function runOcr(file){if(!file)throw Error('Aucun fichier.');const data=await file.arrayBuffer();let s='';const bytes=new Uint8Array(data);for(let i=0;i<bytes.length;i+=0x8000)s+=String.fromCharCode(...bytes.subarray(i,i+0x8000));const r=await fetch('/api/ocr',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'include',body:JSON.stringify({name:file.name,mime:file.type,dataBase64:btoa(s)})});if(!r.ok)throw Error((await r.json().catch(()=>({}))).error||'OCR indisponible');return r.json()}
